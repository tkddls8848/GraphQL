type Movie {
    id:Int!,
    title:String!,
    rating:Float!,
    summary:String,
    language:String,
    genres:String,
    medium_cover_image:String!
}

type Query {
    Movies(limit:Int, rating:Float): [Movie]!
}
